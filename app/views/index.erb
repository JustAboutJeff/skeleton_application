<div class="container">

  <% if logged_in? %>
    <ul>
    <% @users.each do |user| %>
      <li> <%= user.name %> || <%= user.email %> || <%= user.password %></li>
    <% end %>
    </ul>
  <% else %>
    <h1>Welcome!</h1>
  <% end %>

</div>
